[project]
name = "environmental-streaming-platform"
version = "0.1.0"
description = "A streaming data platform for real-time environmental measurements."
requires-python = ">=3.11"

[tool.ruff]
line-length = 88

[tool.pytest.ini_options]
testpaths = ["tests"]
